package _GUI._01_JFrame;
import java.awt.*;
public class D1029_4_컨테이너배치 {

	public static void main(String[] args) {
/*
		배치관리자 (Layout Manager)
			컴포넌트 위치를 알아서 지정해줌...
			내가 원하는대로 하려면 위치를 따로 값으로 지정해줘야됨다.
			FlowLayout
			FlowLayout(int align) align=>정렬
			FlowLayout(int align, int hgap수평, int vgap수직 => default는 5로 되어있음.)
			new FlowLayout(FlowLayout.LEFT,30,40) =>왼쪽 정렬
			
			
		Container 클래스의 메소드
			setLayout(LayoutManager mgr)
			
			FlowLayout.맨위부터 순서대로 
			BorderLayout.경계를 지정해서 지정(경계 5가지=> 동서남북, 가운데) 경계를 생략하면 center로 들어감.
			GridLayout.표처럼 생긴것. flow와 비슷하지만 이것은 개수를 지정할수있음.(2*5) (3*4) =>계산기
			CardLayout.겹쳐져있음.
		
		
		
		
		*/
	}

}
